# D-Chipset App - Simple To Do List Feature

This is a simple to-do list feature for the D-Chipset App.
## Installation
To run this application, you need to have Python and pip installed. Then, install the required packages by running:
```
pip install -r requirements.txt
```## Running the Application
To start the application, simply run:
```
streamlit run app.py
```